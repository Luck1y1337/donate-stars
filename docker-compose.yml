services:
  bot:
    build: .
    container_name: luck1y_donate_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/bot.db
    volumes:
      - bot_data:/data

volumes:
  bot_data:
